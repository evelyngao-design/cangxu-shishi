# GitHub 网页端更新文件教程（不用 GitHub Desktop）

> 适合场景：代码有更新，想把最新文件同步到 GitHub 仓库。全程在浏览器里完成。

---

## 第一步：打开你的仓库

1. 浏览器访问 **https://github.com**，确认已登录
2. 点击右上角你的头像 → **Your repositories**（你的仓库）
3. 点击 **`cangxu-shishi`** 仓库进入
4. 你会看到仓库里的文件列表

---

## 第二步：上传最新文件（自动覆盖旧文件）

1. 在文件列表上方，找到 **"Add file"**（添加文件）按钮（绿色，在 "Code" 按钮旁边）
2. 点击它，选择 **"Upload files"**（上传文件）
3. 页面会变成一个拖拽上传区域（写着 "Drag files here to add them to your repository"）

4. 打开 Finder，进入**"下载" → `cangxu-shishi`** 文件夹

5. **先显示隐藏文件**：在 Finder 窗口里按快捷键 **`Cmd + Shift + .`**（句号）
   - 你会看到出现一个灰色的 `.nojekyll` 文件
   - 这一步很重要，否则 `.nojekyll` 不会被上传

6. 选中文件夹里的**所有文件**（按 `Cmd + A` 全选），然后**直接拖到**浏览器的上传区域
   - 需要拖的文件：`index.html`、`app.js`、`cloud.js`、`colors_and_type.css`、`supabase-setup.sql`、`README.md`、`.nojekyll`、两个 `.md` 教程文件、`cangxu-shishi.design`
   - 空的 `assets` 文件夹不用管（空文件夹 GitHub 会自动忽略）

7. 等待上传完成，文件列表会显示每个文件：
   - 已存在的文件显示 **"Update"**（更新）
   - 新文件显示 **"Add"**（新增）
   - 进度条都变成绿色对勾就表示上传完了

---

## 第三步：提交更改

1. 上传完成后，页面下方有 **"Commit changes"**（提交更改）区域
2. 第一个输入框填写提交说明，比如：
   `更新：修复云端登录、新增本地数据持久化和云端同步`
3. 第二个输入框（可选描述）留空
4. 保持选中 **"Commit directly to the main branch"**（直接提交到 main 分支）
5. 点击绿色的 **"Commit changes"** 按钮
6. 页面跳回仓库文件列表，文件名旁边的最新提交信息会变成你刚写的说明

---

## 第四步：删除旧的多余文件（重要）

你最早上传的版本里有一个 `pages` 文件夹（旧版多页面结构），现在已经用不到了，需要删掉，否则会和新版混淆。

1. 在仓库文件列表中，检查是否有 **`pages`** 文件夹
2. 如果**没有**，跳过这一步
3. 如果**有**：
   - 点击进入 `pages` 文件夹
   - 里面应该有 `bookkeeping.html`、`inventory.html`、`diet.html`、`profile.html` 等旧文件
   - 逐个删除：点击文件名进入 → 点击文件内容右上角的 **垃圾桶图标 🗑️**（Delete this file）→ 拉到页面底部点 **"Commit changes"**
   - 把文件夹里的文件全部删完后，`pages` 文件夹会自动消失

> 小技巧：每个文件删除都要单独提交一次，这是 GitHub 网页端的限制，耐心点几下即可。

---

## 第五步：确认 .nojekyll 上传成功

1. 回到仓库根目录文件列表
2. GitHub 默认不显示以 `.` 开头的文件，但你可以在文件列表上方的搜索框输入 `.nojekyll` 搜索
3. 如果能搜到，说明上传成功
4. 如果搜不到（比如拖拽时漏掉了）：
   - 点击 **"Add file" → "Create new file"**（创建新文件）
   - 在文件名输入框里输入：`.nojekyll`
   - 正文区域**完全留空**
   - 拉到底部点击 **"Commit changes"**

---

## 第六步：等待网站自动更新

1. 提交完成后，GitHub Pages 会在 **1-3 分钟内**自动重新部署
2. 打开你的网站网址：`https://你的用户名.github.io/cangxu-shishi/`
3. 如果看到的还是旧版本：
   - 按 **`Cmd + Shift + R`**（Mac）强制刷新，绕过缓存
   - 手机上：关掉浏览器标签页重新打开，或清除浏览器缓存
   - 再等 2 分钟刷新

---

## 以后每次更新都是同样的流程

记住这个简单循环：

1. 我给你更新了代码 → 下载文件夹里的文件变成最新
2. GitHub 仓库 → **Add file → Upload files**
3. 全选下载文件夹里的文件拖进去（记得先按 `Cmd + Shift + .` 显示隐藏文件）
4. 写提交说明 → **Commit changes**
5. 等 1-3 分钟，强制刷新网页

---

## 常见问题

**Q：拖拽上传时提示文件太大？**
A：GitHub 网页端单文件限制 100MB，我们的文件最大才 100 多 KB，不会遇到。

**Q：可以只上传改动过的文件吗？**
A：可以，但没必要。把所有文件都拖上去，GitHub 会自动判断哪些更新了、哪些没变，没变的不会产生多余提交。

**Q：提交后网站显示 404 或样式错乱？**
A：1）确认根目录有 `index.html`；2）确认 `.nojekyll` 存在；3）等 3 分钟后强制刷新；4）检查 Settings → Pages 里 Branch 是否还是 `main` / `(root)`。

**Q：怎么确认线上跑的是最新版？**
A：在网站上按 F12 打开控制台（电脑端），或者直接测试最新功能——比如点云朵图标看是否能弹出登录界面。能弹出就说明是新版。

**Q：中文文件名（教程.md）上传有问题吗？**
A：没问题，GitHub 完整支持中文文件名，不影响网站运行。

---

卡在哪一步了随时告诉我，描述一下你看到的画面即可。
