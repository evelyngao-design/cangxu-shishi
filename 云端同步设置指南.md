# 仓序食时 - 云端同步超详细新手教程

> 不用怕！整个过程大约需要 10-15 分钟，跟着一步步做就行。遇到问题随时问我。

---

## 一、先了解一下什么是"云端同步"

**不配置云端**：你的数据只存在当前这个浏览器里。换电脑、换浏览器、清缓存，数据就没了。手机和电脑数据不通。

**配置云端后**：
- 电脑录入的数据，手机上打开网页登录同一账号就能看到
- 手机上记的账，回家打开电脑也能看到
- 数据自动上传，不用担心丢失
- 完全免费（Supabase 免费额度足够个人使用）

---

## 二、注册 Supabase 账号（约 3 分钟）

### 步骤 1：打开 Supabase 官网
1. 在浏览器地址栏输入：`https://supabase.com`，按回车
2. 点击右上角的 **"Start your project"** 按钮（绿色或深蓝色按钮）
3. 你会看到登录选项：
   - 如果你有 GitHub 账号：点击 "Continue with GitHub"（最快）
   - 如果没有：点击下方用邮箱注册，填写邮箱和密码即可

> 💡 如果你没有 GitHub 也不想注册，其实用邮箱注册 Supabase 也可以。但 GitHub 注册更方便，推荐花 1 分钟注册一个 GitHub 账号。

### 步骤 2：创建新项目
1. 登录后，你会看到一个 **"New Project"** 按钮，点击它
2. 填写项目信息：
   - **Name**（项目名）：填 `cangxu-shishi`（或者随便填，自己能认出就行）
   - **Database Password**（数据库密码）：输入一个你能记住的密码（比如 `Cangxu123!`，至少 8 位），**请记下来**，虽然平时用不到但万一要改数据库需要
   - **Region**（区域）：下拉选择 **Singapore**（新加坡，离香港最近，速度最快）
   - **Pricing Plan**：选 **Free**（免费，默认就是这个）
3. 点击右下角 **"Create new project"**
4. 这时会显示一个加载页面，说 "Setting up project..."，**等 2-3 分钟**，不要关闭页面
5. 看到项目仪表盘（Dashboard）出来了，就说明创建成功了

---

## 三、在数据库里建表（约 3 分钟）

这一步是告诉 Supabase 要存哪些数据。不用担心 SQL，我已经帮你写好了，你只需要复制粘贴。

### 步骤 1：打开 SQL Editor
1. 看页面最左侧的菜单栏（可能需要先点左上角的菜单图标 ≡）
2. 找到并点击 **"SQL Editor"**（图标是一个方框里有 `>_`）
3. 点击 **"New query"** 按钮（在页面中间或右上角）
4. 你会看到一个空白的编辑区域

### 步骤 2：复制建表代码
1. 在电脑上，打开你下载的 `cangxu-shishi` 文件夹
2. 找到名为 **`supabase-setup.sql`** 的文件
3. 用文本编辑器打开它（Mac 上双击就会用"文本编辑"打开，或者右键→打开方式→文本编辑）
4. 按 `Cmd + A` 全选，然后 `Cmd + C` 复制所有内容

### 步骤 3：粘贴并执行
1. 回到浏览器的 Supabase SQL Editor 页面
2. 把刚才复制的内容粘贴到空白编辑区（`Cmd + V`）
3. 点击右下角的 **"Run"** 按钮（绿色三角形），或者按 `Cmd + Enter`
4. 等几秒钟，看到下方出现 "Success. No rows returned" 就说明成功了
5. 如果你看到红色错误信息，请截图发给我，我帮你看是什么问题

> ⚠️ 注意：不要修改 SQL 里的任何内容，直接全部复制粘贴运行就行。

---

## 四、获取连接信息（约 2 分钟）

现在你需要拿到两个东西：**Project URL** 和 **anon key**，相当于你数据库的"地址"和"钥匙"。

### 步骤 1：进入 API 设置页面
1. 看左侧菜单栏最底部，点击 **齿轮图标 ⚙️**（Settings）
2. 在展开的菜单中点击 **"API"**
3. 页面会显示 "API Settings"

### 步骤 2：复制 Project URL
1. 在页面中找到 **"Project URL"** 这一栏
2. 它的值看起来像这样：`https://abcdefghijk.supabase.co`（一串字母+supabase.co）
3. 点击它右边的 **复制图标**（📋 或两个重叠的方框），或者手动选中复制
4. **先粘贴到一个备忘录里备用**，或者先开着仓序食时网页等下直接粘贴

### 步骤 3：复制 anon public key
1. 在同一页面往下滚动，找到 **"Project API keys"** 区域
2. 找到标注为 **"anon public"** 的那一行（注意！不是 `service_role`！）
3. 这是一个很长的字符串，以 `eyJhbGciOi...` 开头
4. 点击它右边的复制图标复制
5. **也粘贴到备忘录备用**

> ⚠️ 重要提醒：
> - ✅ 用 `anon public` key（这个是给浏览器用的，安全）
> - ❌ 不要用 `service_role` key（这个有管理员权限，泄露了别人能删你数据）
> - anon key 是公开的，别人拿到也只能操作自己的数据（因为有 RLS 保护），不用太担心

---

## 五、在仓序食时中配置（约 2 分钟）

### 步骤 1：打开配置弹窗
1. 打开你下载的 `cangxu-shishi` 文件夹，双击 `index.html` 用浏览器打开
2. 看页面**右上角**，在铃铛图标 🔔 旁边，有一个**云朵形状的图标 ☁️**
3. 点击这个云朵图标，会弹出"配置云端同步"的窗口

### 步骤 2：粘贴连接信息
1. 第一个输入框 "Supabase Project URL"：粘贴你刚才复制的 Project URL（`https://xxx.supabase.co`）
2. 第二个输入框 "Supabase Anon (public) Key"：粘贴你复制的 anon key（以 eyJ 开头的长字符串）
3. 点击 **"保存并连接"** 按钮

### 步骤 3：注册账号
1. 保存后，页面会跳转到登录界面
2. 点击 **"注册新账号"** 按钮上方，先输入你的邮箱和密码：
   - 邮箱：填你常用的邮箱（可以收邮件的）
   - 密码：至少 6 位，比如你常用的密码
3. 点击 **"注册新账号"**
4. 如果页面提示"注册成功！请查收邮件验证后登录"：
   - 去你的邮箱，找到 Supabase 发来的验证邮件（可能在"垃圾邮件"或"推广"文件夹里）
   - 点击邮件里的确认链接
   - 回到网页，重新输入邮箱密码，点击"登录"
5. 如果直接登录成功，你会看到页面跳转到首页，右上角出现绿色的"已同步"标志

---

## 六、测试一下同步是否成功

### 在电脑上添加一条数据
1. 登录后，点击"记一笔"快捷操作
2. 随便录入一条采购记录（比如买了牛奶），保存
3. 等 2-3 秒（看右上角云朵图标旁边的字变成"已同步"）

### 在手机上测试
1. 首先你需要把网页发布到 GitHub Pages（之前讨论过的步骤），或者用其他方式让手机能访问到这个网页
2. 手机浏览器打开你的网页地址
3. 点击右上角云朵图标，填入**同样的 URL 和 Key**（和电脑上一样的）
4. 用**同一个邮箱密码**登录
5. 你应该能看到刚才在电脑上录入的牛奶记录了！
6. 在手机上再录一条数据，回到电脑刷新页面，也能看到手机录的数据

---

## 七、日常使用

配置好之后，日常使用完全不需要额外操作：
- 打开网页自动登录（浏览器会记住登录状态）
- 每次录入/修改/删除数据，2 秒后自动同步到云端
- 右上角云朵图标显示当前同步状态：
  - 🟢 绿色云朵 + "已同步"：一切正常
  - 🔵 蓝色旋转图标 + "同步中"：正在上传数据
  - 🔴 红色 + "同步失败"：网络问题或配置有误，检查网络或重新配置
- 在"我的"页面可以看到"云端同步"区域，显示当前登录邮箱，可以手动点"立即同步"或退出登录

---

## 常见问题

**Q：注册后没收到验证邮件？**
A：1）检查垃圾邮件/推广邮件文件夹；2）在 Supabase 后台 Authentication → Users 里可以看到你的账号，可以手动点确认；3）如果是自己用其实不验证也能用，但建议验证一下更安全。

**Q：点"保存并连接"后报错？**
A：最常见的原因：
- URL 复制错了（应该是 `https://xxx.supabase.co` 结尾，不要多复制空格或斜杠）
- Key 复制错了（确认是 anon public 不是 service_role）
- SQL 没有运行成功（回到第三步确认 SQL 已执行成功）

**Q：免费额度够用吗？会收费吗？**
A：Supabase 免费版提供 500MB 数据库空间和 5GB 月流量，对于个人记账来说完全够用（一条记录才几百字节，500MB 可以存几十万条记录）。不会自动收费，超出免费额度会暂停服务，不会扣钱。

**Q：如果我以后不想用云端了怎么办？**
A：在"我的"→"云端同步"→"退出登录"，或者点云朵图标→"断开云端连接，仅本地使用"，数据会保留在本地，只是不再同步。

**Q：我分享链接给朋友，他能看到我的数据吗？**
A：不能。他打开链接后需要自己注册/登录，只能看到自己的数据。你和他的数据是完全隔离的。

**Q：手机上需要安装 App 吗？**
A：不需要。手机浏览器打开网页就行。可以把网页"添加到主屏幕"，用起来就像 App 一样（iPhone 用 Safari，安卓用 Chrome 都有这个功能）。

**Q：数据安全吗？**
A：Supabase 是行业知名的后端服务（类似给小公司用的 Firebase），数据存在他们的云数据库里，传输加密。加上我们设置了 RLS 行级安全策略，每个用户只能访问自己的数据，技术上是安全的。但如果是极其敏感的财务数据，建议慎重考虑是否使用云端。

---

## 遇到问题怎么办？

如果哪一步卡住了，直接告诉我：
1. 你做到了第几步
2. 看到了什么错误提示（最好截图）
3. 我会帮你排查

不用有压力，配置只需要做一次，之后就不用管了。
